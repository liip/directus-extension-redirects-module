{
	"name": "directus-extension-redirects-module",
	"version": "1.0.0",
	"keywords": [
		"directus",
		"directus-extension",
		"directus-custom-pack"
	],
	"directus:extension": {
		"type": "pack",
		"host": "^9.21.2"
	},
	"scripts": {
		"build": "(cd interface && directus-extension build) && (cd endpoint && directus-extension build)",
		"dev:interface": "cd interface && directus-extension build -w --no-minify",
		"dev:endpoint": "cd endpoint && directus-extension build -w --no-minify"
	},
	"devDependencies": {
		"@directus/extensions-sdk": "9.21.2"
	}
}
